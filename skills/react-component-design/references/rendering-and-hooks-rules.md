# Rendering And Hooks Rules

- Memoization should be evidence-driven, not default.
- Effects should synchronize with external systems, not replace state design.
- Keep client-side state narrow and derive values when possible.
