# Branch Topology Decision Rules

- Prefer simple topology until release/hotfix needs justify extra branch classes.
- Separate long-lived and short-lived branches with clear ownership.
- Use release branches only when stabilization must be decoupled from mainline delivery.
- Keep merge direction one-way where possible to reduce confusion.
