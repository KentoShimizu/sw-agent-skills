# ORM Loading And Session Rules

- Choose eager/lazy loading based on actual query access patterns.
- Keep session lifecycle short-lived and explicit.
- Validate generated SQL for critical queries, not only ORM code shape.
