# Revert Scope Selection

- Revert the smallest scope that restores stability.
- Prefer explicit revert commits over destructive history rewrites on shared branches.
- If rollback does not restore expected behavior, escalate to incident/root-cause workflow.
