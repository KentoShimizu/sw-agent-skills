# Rebase Safety Rules

- Never rebase shared protected branches.
- Rebase only when history rewrite policy permits force-with-lease.
- If PR is already open, use PR sync policy unless repository explicitly allows rebase.
