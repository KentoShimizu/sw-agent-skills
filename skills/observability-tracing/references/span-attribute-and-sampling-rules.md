# Span Attribute And Sampling Rules

- Standardize span attributes for service, operation, and error context.
- Keep sampling policy high enough to debug critical incidents.
- Ensure trace IDs propagate across asynchronous boundaries where possible.
