# Reproducibility Metadata Rules

- Every run must capture code, data, and config versions.
- Metrics without artifact lineage are not decision-grade.
- Re-run reproducibility should be testable from tracked metadata alone.
