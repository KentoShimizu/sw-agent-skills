# Drift Alerting Escalation Rules

- Alert thresholds must be actionable, not purely statistical noise.
- Each severity level needs explicit owner and response SLA.
- Retraining should be policy-driven, not automatic by default.
